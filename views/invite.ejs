<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join Our Discord Server</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #36393f;
            color: #fff;
        }

        .container {
            max-width: 400px;
            padding: 40px;
            background-color: #2f3136;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            color: #7289da;
            font-size: 24px;
            margin-bottom: 20px;
        }

        p {
            font-size: 16px;
            line-height: 1.6;
            margin-bottom: 30px;
        }

        .discord-button {
            display: inline-block;
            background-color: #7289da;
            color: #fff;
            font-size: 16px;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .discord-button:hover {
            background-color: #5f73a1;
        }

        .discord-logo {
            vertical-align: middle;
            margin-right: 10px;
        }

        input {
            display: none;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Join <%= server.serverName %>
        </h1>
        <p>Click the button below to join our Discord community. Chat with other members and more!</p>
        <form id="joinForm" action="/acceptInvite" method="POST">
            <input name="serverId" type="text" value="<%= server._id %>">
            <button id="joinButton" class="discord-button" type="submit">
                Join Server
            </button>
        </form>
    </div>
</body>
<script>
    let form = document.querySelector('form')
    form.addEventListener('submit', () => {
        setTimeout(() => {
            window.location.href = '/directMessage'
        }, 2000);
    })
</script>

</html>